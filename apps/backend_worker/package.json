{
  "name": "@template/backend-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@template/api": "*"
  },
  "devDependencies": {
    "@template/typescript-config": "*"
  }
}