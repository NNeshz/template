{
  "name": "@template/backend_worker",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "check-types": "tsc --noEmit",
    "clean-workspace": "rm -rf node_modules"
  },
  "dependencies": {
    "@template/api": "*",
    "elysia": "^1.4.27"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
