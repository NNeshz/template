{
  "name": "@template/auth",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "generate": "bunx --env-file=../../.env @better-auth/cli@latest generate --output ./../../packages/database/src/schema.ts -y",
    "clean-workspace": "rm -rf node_modules"
  },
  "dependencies": {
    "@template/database": "*",
    "better-auth": "^1.4.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "5.5.4"
  }
}
